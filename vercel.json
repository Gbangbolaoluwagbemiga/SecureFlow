{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "corepack enable && pnpm -C frontend install --frozen-lockfile=false && pnpm -C frontend build",
  "installCommand": "corepack enable && pnpm -C frontend install --frozen-lockfile=false",
  "outputDirectory": ".next",

  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}
